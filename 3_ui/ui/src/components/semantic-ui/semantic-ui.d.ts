
interface JQuery {
    dropdown(config?:any);
    dropdown(behavior?:String, value?:any);
    popup(config?:any); // http://semantic-ui.com/modules/popup.html#/examples

    sidebar(config?: any);
    modal(config?: any);
}
