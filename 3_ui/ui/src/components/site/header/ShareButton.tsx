import * as css from './SiteHeader.css';

export const ShareButton = () => (
    <span className={css.shareButton}
          data-tooltip="Share (NYI)"
          data-position="bottom center">
        Share
    </span>);
