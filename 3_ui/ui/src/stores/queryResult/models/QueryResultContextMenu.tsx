

import { QueryResult } from "./QueryResult";

export class QueryResultContextMenu {
	static menuItemsFor(qr: QueryResult) {
		const {currentView} = qr;
		switch (currentView) {
			case 'pivot': {

			}
		}

		return (<div>

		</div>).props.children
	}
}