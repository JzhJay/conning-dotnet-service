((`advise;1); (`jsetb;  `name `".*.k.p";  (`index;());  (`sxy;   700 600)))
((`advise;1); (`jsetb;  `name `".*.k.p";  (`index;());  (`sloc;   200 50)))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_1);  (`kd;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss;   ,`C_1 `R_1)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_1);  (`ku;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_1);  (`k;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss.assert;   ,`C_1 `R_1)))
(; (`getValue; ((`advise;1);".k.p.cross..j.s";".tc.crossSelection")))
(; (`assert; ({.tc.crossSelection~,`C_1 `R_1})))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_1);  (`value;"999")))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_1);  (`value.assert;"999")))
(; (`getValue; ((`advise;1);".k.p.cross.C_1.R_1";".tc.crossValue")))
(; (`assert; ({.tc.crossValue=999})))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_2 `R_2);  (`kd;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss;   ,`C_2 `R_2)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_2 `R_2);  (`ku;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_2 `R_2);  (`k;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss.assert;   ,`C_2 `R_2)))
(; (`getValue; ((`advise;1);".k.p.cross..j.s";".tc.crossSelection")))
(; (`assert; ({.tc.crossSelection~,`C_2 `R_2})))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_2 `R_2);  (`value;"888")))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_2 `R_2);  (`value.assert;"888")))
(; (`getValue; ((`advise;1);".k.p.cross.C_2.R_2";".tc.crossValue")))
(; (`assert; ({.tc.crossValue=888})))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_0 `R_0);  (`kd;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss;   (`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5))))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_0 `R_5);  (`ku;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss.assert;   (`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5))))
(; (`getValue; ((`advise;1);".k.p.cross..j.s";".tc.crossSelection")))
(; (`assert; ({.tc.crossSelection~(`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5)})))

((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_1 `R_5);  (`kd;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss;   (`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5;    `C_1 `R_0;    `C_1 `R_1;    `C_1 `R_2;    `C_1 `R_3;    `C_1 `R_4;    `C_1 `R_5))))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;   `C_0 `R_0);  (`ku;)))
((`advise;1); (`jsetb;  `name `.k.p.cross;  (`index;());  (`ss.assert;   (`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5;    `C_1 `R_0;    `C_1 `R_1;    `C_1 `R_2;    `C_1 `R_3;    `C_1 `R_4;    `C_1 `R_5))))
(; (`getValue; ((`advise;1);".k.p.cross..j.s";".tc.crossSelection")))
(; (`assert; ({.tc.crossSelection~(`C_0 `R_0;    `C_0 `R_1;    `C_0 `R_2;    `C_0 `R_3;    `C_0 `R_4;    `C_0 `R_5;    `C_1 `R_0;    `C_1 `R_1;    `C_1 `R_2;    `C_1 `R_3;    `C_1 `R_4;    `C_1 `R_5)})))

((`advise;1); (`jsetb;  `name `".*.k.p";  (`index;());  (`close;())))
